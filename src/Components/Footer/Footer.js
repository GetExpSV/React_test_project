import React from 'react';
import footer_class from './Footer.module.css';

const Footer = (props) => {
    return (
        <div className={footer_class.footer}>Footer</div>
    )
}

export default Footer;